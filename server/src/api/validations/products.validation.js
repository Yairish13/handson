const Joi = require('joi');

module.exports = {
    // filterByTitleValidation: {
    //     query: Joi.object({
    //         title: Joi.string().min(1).required(),
    //     }),
    // },
    // filterByTimeValidation: {
    //     query: Joi.object({
    //         from: Joi.string().min(1),
    //         to: Joi.string().min(1),
    //     })
    // },
    // filterValidation: {
    //     query: Joi.object({
    //         search: Joi.string().min(1).required(),
    //     })
    // }
};
